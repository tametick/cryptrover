{
  "name": "cryptrover-web",
  "version": "1.0.0",
  "description": "CryptRover - Terminal roguelike ported to TypeScript/browser",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python3 -m http.server 8080",
    "dev": "concurrently \"tsc --watch\" \"python3 -m http.server 8080\"",
    "test": "tsc && node dist/tests/harness.js",
    "itch": "npm run build && rm -rf itch-build && mkdir itch-build && cp index.html style.css itch-build/ && cp -r dist media itch-build/ && butler push itch-build stellarcartography/crypt-rover:html5"
  },
  "keywords": [
    "roguelike",
    "game",
    "typescript"
  ],
  "author": "Ido Yehieli",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
